完成代码重构工作。
总体介绍:
我现在已经有了代码：
D:\PythonProject\Pulse_detection\detecter_baseband\former_script文件夹下的内容用于从脉冲星的基带数据中进行巨脉冲搜索。
各个文件作用：
vdif2psrfits_v16.py用于将单个vdif文件处理，输出为多个psrfits文件，vdif2psrfits_v16_p51223_ur_no0002_m5a.ini为vdif2psrfits_v16.py的配置文件，负责提供vdif信息，定义各种转换参数。
dm_cor内的dm_cor_psrfits_latest_25-12-12.py用于对psrfits文件进行消色散处理，输入原始的psrfits文件和已知的DM值，参考频率，输出校正后的psrfits文件【将其称为色散校正模块：输入一个原始psrfits文件，对应的DM值，参考频率，输出校正后的psrfits文件】
Pulse_detection_v4.1_ur.py用于探测经过DM校正的psrfits文件中含有的潜在的巨脉冲，返回探测结果，【将其称为探测模块，即输入Psrfits文件，模块进行脉冲探测，输出结果】

当前的使用方法/步骤：
假设有一个vdif格式的观测文件
步骤一：使用vdif2psrfits_v16.py配合vdif2psrfits_v16_p51223_ur_no0002_m5a.ini，由一个vdif文件获得多个psrfits文件，输出/存储到一个文件夹中
步骤二：使用dm_cor_psrfits_latest_25-12-12.py，将步骤一获得的未经过DM校正的psrfits文件进行DM校正，DM值是已知的，参考频率也是已知的（由于dm_cor_psrfits_latest_25-12-12.py一次只能处理单个psrfits文件，所以额外的使用detecter_baseband/dm_cor/DM_cor_no002.sh来使其处理文件夹下的所有psrfits文件），最终获得DM校正后的psrfits文件
步骤三：由步骤二将获得的含有DM校正的psrfits文件夹，使用Pulse_detection_v4.1_ur.py对文件夹下的所有psrfits文件进行探测，最终结果输出为一个csv文件，处理由此结束

总结：vdif2psrfits_v16.py，dm_cor_psrfits_latest_25-12-12.py，Pulse_detection_v4.1_ur.py三个脚本实际上是一个完整的从vdif文件中进行单脉冲搜索的pipline，三者通过对psrfits文件的输出/输入实现单脉冲搜索功能（即vdif2psrfits_v16.py输入vdif,输出原始psrfits,而dm_cor_psrfits_latest_25-12-12.py输入的上一步产生的原始psrfits，输出新的经过DM校正的psrfits,Pulse_detection_v4.1_ur.py输入上一步产生的DM校正的psrfits,输出脉冲结果）
现有的运行逻辑存在的问题：当前的运行逻辑是，从vdif文件中按照.ini定义的规则将vdif文件转为多个psrfits，完成后再将所有的psrfits进行DM修正，再获得新的所有psrfits，再使用探测脚本遍历所有的psrfits，也就是经过三步，每步都要处理大量的psrfits,同时过程中要保存每一步产生的psrfits才能被下一步读取/处理

修改目标：
纵观以上pipeline,可以考虑将以上dm_cor_psrfits_latest_25-12-12.py，Pulse_detection_v4.1_ur.py三个脚本的功能整合到vdif2psrfits_v16.py,得到一个新的脚本，直接对vdif文件进行处理，输出最终的csv探测结果,处理过程中不产生过多的psrfits文件，模块间的数据i/o都在内存中完成，即vdif2psrfits_v16.py在内存中每产生一个psrfits对象（还没有被输出为psrfits文件），就先经过校正模块和探测模块处理，最终，仅输出含有脉冲的的psrfits文件

我的修改思路：
先将dm_cor_psrfits_latest_25-12-12.py，Pulse_detection_v4.1_ur.py脚本重写，得到可供调用的色散校正模块dm_correction_module和探测模块pulse_detection_module
对于这两个模块，功能是：
在初始化阶段读取.ini文件后，先将获得各模块的处理参数，
dm_correction_module的功能：输入一个psrfits数据对象，对其进行DM校正，输出校正后的psrfits数据对象
即：
psrfits_dm_corrected=dm_correction_module（raw_psrfits,从ini文件获得的参数）

pulse_detection_module的功能：输入一个psrfits数据对象，进行脉冲探测，输出探测结果（是否探测到脉冲，脉冲的snr值，脉冲的TOA，脉冲宽度）
即：
result=pulse_detection_module的（psrfits_dm_corrected，从ini文件获得的参数）
                        
创建了以上两个功能模块后，我事先仔细阅读vdif2psrfits_v16.py的运行流程，注意到在vdif2psrfits_v16.py中，经过了subints累计，再写入fits文件的过程（对应函数write_psrfits_file_multiple_subints的功能），在程序向磁盘输出fits文件前，实际上是将一个变量hdulist存储为fits文件，所以可以推迟向本地磁盘存储fits文件的时机，将色散校正模块和探测模块依次插入到向磁盘输出fits文件前的步骤，也就是将这个hdulist依次经过校正模块和探测模块处理，当探测到脉冲存在时，才执行以后的的写入文件的操作，否则跳过输出fits文件写入步骤，继续按vdif2psrfits_v16.py中的流程进行得到下一个fits的步骤

详细解释,具体实现方法：
vdif2psrfits_v16.py中，可见是由write_psrfits_file_multiple_subints函数完成psrfits文件的输出的，所以在write_psrfits_file_multiple_subints函数中，插入色散校正模块和探测模块，修改后write_psrfits_file_multiple_subints的伪代码是：
原始代码：
   # Create HDU list and write file (single-table format)
    hdulist = fits.HDUList([primary_hdu, table_hdu])    #得到了待存储为fits的的hdulist
    hdulist.writeto(output_file, overwrite=True, checksum=True)
    print(f"Written single-table PSRFITS: {output_file} with {n_subints} subints and {actual_nchans} channels")
    if global_offset > 0:
        print(f"  Global time offset applied: {global_offset:.6f} seconds")

修改后伪代码：
    # Create HDU list and write file (single-table format)
    hdulist = fits.HDUList([primary_hdu, table_hdu])
    # ① 色散校正模块
    corrected_hdulist = dm_correction_module(hdulist, DM_value, reference_frequency)
    # ② 探测模块
    pulse_detected_result = pulse_detection_module(corrected_hdulist, snr_threshold，手动屏蔽特定频率列表，peak_distance等参数)
    （DM_value, reference_frequency, snr_threshold, freq_range，手动屏蔽特定频率列表等参数可以从.ini文件中读取）

    # 仅写入含有脉冲的hdulist为fits文件
    if  pulse_detection_module在corrected_hdulist中探测到脉冲（pulse_detected_result中提示有探测到的脉冲）:
        将结果添加到最终的csv文件中（可以在内存中维护一个列表，最后一次性写入csv文件）
        向指定路径输出对应的psrfits文件（原始的hdulist和经过DM校正的corrected_hdulist，以及原始hdulist对应的vdif片段的vdif文件）

这样的话，原write_psrfits_file_multiple_subints的原有功能发生了改变，原来是无条件地输出psrfits文件，现在是有条件的输出psrfits文件（当探测模块探测到脉冲存在时才输出），同时在输出之前，先经过色散校正模块和探测模块的处理，最终输出的psrfits文件是经过色散校正和未经过色散校正的，并且是含有脉冲的psrfits文件

特别注意：在vdif2psrfits_v16.py中，在hdulist = fits.HDUList([primary_hdu, table_hdu])语句前的数据处理代码，尤其是如何读取vdif文件，进行一系列操作，VDIF从读取/seeking/FFT处理...这些你都不用管。你只需要由hdulist = fits.HDUList([primary_hdu, table_hdu])得到的hdulist施加两个额外的模块进行分析，至于这个hdulist是什么来的，相关的代码不用动，你必须原封不动的复制，只在hdulist创建后插入集成的 DM 校正和脉冲检测


最终，你的脚本也输出Pulse_detection_v4.1_ur.py一样的命名方式以时间戳结尾csv文件（这时候就没有原始算法输出的csv文件的FITS_File列了，也不需要，Coarse_Index,Coarse_Rel_Time_ms,Coarse_Abs_MJD,Coarse_UTC_Time,Precise_Rel_Time_ms。）

vdif转psrfits数据结构依然按照原来和vdif2psrfits_v16.py配套.ini文件中定义的转换参数进行，实际上新的脚本的ini文件中将会多一些参数，用于色散校正模块和探测模块，如dm校正也需要事先手动输入DM值和参考频率，脉冲搜寻同原始的Pulse_detection_v4.1_ur.py一样，需要事先手动指定阈值

另外由于要保存含有脉冲的，未经校正和校正后的psrfits文件，所以新建两个输出地址参数，用于设置对于找到脉冲的psrfits数据对象，在本地指定路径下保存原始psrfits文件和校正后的psrfits文件，原始的vdif片段
所以新的脚本依然使用.ini文件，只不过多了参数（DM值和参考频率（当参考频率为空时，默认为ini文件中的观测中心频率），探测模块的流量和幅度snr阈值,手动频率特定频率的范围，csv文件和符合条件的psrfits文件输出地址）

另外检查新的脚本的实现方式在TOA的计算上是否一致

在功能实现上，你可以新建.py文件定义多个模块，由一个脚本引用这些模块完成目标
你生成的脚本放在一个独立的文件夹integrated_pipeline中

对于新的脚本，我已经给你设计好了一个ini文件，你的脚本将加载这个ini文件：
[paths]
# VDIF / Mark5B input file path
vdif_file = /home/lyb/lyb_workspace/antenna_data/VDIF/IAA_VDIF/r6326e_bv_326-1702_ch01.vdif_7183.vdif
data_format = vdif



[observation]
sample_rate = 1024*u.MHz
telescope = Badary
source_name = B0531+21
dm_source = 56.79
t_start = 0.0

# Reference time for Mark5B format (required when data_format=mark5b)
ref_time = 2025-12-23T17:05:00.0

# File number to start from (for resuming)
start_file = 0

[output]
max_subints_per_file = 60
max_files = 99999
version = 0

[frequency]
# Overall center frequency for all subbands
center_freq = 2419.8
subbands = 0
USB = L
mask_sband = 1
subband_width = 512
withsubband = False

nchan = 1

# Define center frequencies for each subband (for continuous freq axis with gaps)
subband_centers = 2419.8

[processing]
# Number of frequency channels per subband
nchans = 512
# Chunk size (samples per subint)
chunk_size = 1048576
# Time domain downsampling factor
reduction_factor = 1024
# Whether to calibrate bandpass within each subband (B-spline)
calib_bandpass = False
# Number of edge channels to flag per subband
flag_band_edge = 0

[dm_correction]
# DM value is taken from [observation] dm_source

# Reference frequency in MHz; leave empty to use center_freq from [frequency]
ref_freq =

[detection]
# Amplitude SNR threshold for initial peak detection
amp_snr_threshold = 4.0
# Flux SNR threshold for final pulse selection after Gaussian fitting
flux_snr_threshold = 4
# Minimum distance between peaks in samples (to handle pulse shoulders/artifacts)
peak_distance = 5000
# Sigma for frequency-domain RFI removal
sigma_remove_rfi_frequency = 5
# Sigma for time-frequency domain RFI removal
sigma_remove_rfi_time_frequency = 9

[detection_manual_mask]
# Manual frequency mask ranges in MHz
# Format: freq_start,freq_end per line. Leave empty to disable.
# Example:
# freq_mask_ranges = 2200,2210
#                    2280,2290
freq_mask_ranges = 0,2233
                   2510,9999
                   2404.4,2475

[integrated_output]
# CSV output path (base name, timestamp will be appended)
csv_output_path = /home/lyb/lyb_workspace/result_integrated_pipeline/bv_test/pulse_csv_results
# Directory for original PSRFITS files containing detected pulses
output_raw_psrfits_dir = /home/lyb/lyb_workspace/result_integrated_pipeline/bv_test/pulse_psrfits_raw
# Directory for DM-corrected PSRFITS files containing detected pulses
output_corrected_psrfits_dir = /home/lyb/lyb_workspace/result_integrated_pipeline/bv_test/pulse_psrfits_corrected